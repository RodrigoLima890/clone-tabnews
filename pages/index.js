// isso é um componente react
function Home() {
  return (
    <h2>
      Elly, Esse é apenas eu testando uma nova tecnólogia, e usando ela pra
      dizer que de amo :)
    </h2>
  );
}

// o default indica que será o componente padrão a ser exportado
export default Home;
